% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amm_build.R
\name{amm_set_a_uniform}
\alias{amm_set_a_uniform}
\title{Set the uniform additive basis on an AMM builder}
\usage{
amm_set_a_uniform(builder, a)
}
\arguments{
\item{builder}{An object of class \code{amm_builder}.}

\item{a}{One-sided formula or \code{NULL}. Becomes the new base of the
additive component, applied uniformly to every dimension that lacks
an explicit override.}
}
\value{
The modified \code{amm_builder}, returned invisibly for the
  pipe convention but suitable for direct inspection.
}
\description{
Replace the base additive formula of the embedded \code{dims_spec} with
the supplied one-sided formula (or \code{NULL} to disable the additive
component on the base). Per-dimension overrides previously registered
via \code{\link{amm_set_a}} are preserved and continue to take
precedence at their respective indices.
}
\examples{
b <- amm_build(p = 2L) |>
  amm_set_a(k = 2L, ~ x1) |>
  amm_set_a_uniform(~ x1 + x2)
print(b)

}
\seealso{
\code{\link{amm_build}}, \code{\link{amm_set_a}}
}
